run = "npm run build && cd backend && npx tsx src/app.ts"
entrypoint = "backend/src/app.ts"

[deployment]
run = ["sh", "-c", "cd backend && npx tsx src/app.ts"]
build = ["sh", "-c", "npm run build"]

[nix]
channel = "stable-24_05"

[env]
PORT = "3000"
